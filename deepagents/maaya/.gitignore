# Secrets and local config — NEVER commit these
.env
.maaya_config.json
registry.json

# Runtime databases (real user data)
maaya.db
maaya_tracker.db

# Frontend build output (regenerated by npm run build)
frontend/dist/
frontend/node_modules/

# Python runtime
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Temp files created during development
tmp_*.json
